var randNum = Math.round(Math.random() * (10 - min) + min);
var guess = Number (prompt('Guess a num!')); // Convert to Number
//var guess = Number +prompt('Guess a num!'); //
alert(randNum === guess);

var guess = (prompt)